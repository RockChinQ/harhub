import type { WorkspaceRole } from "../../../shared/types";

export const TOKEN_KEY = "harhub.token";
export const WORKSPACE_KEY = "harhub.workspace";

export const roleOptions: WorkspaceRole[] = ["owner", "admin", "member", "viewer"];
